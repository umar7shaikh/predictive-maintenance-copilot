# Screenshots

The main README references two images in this folder:

- `fleet-dashboard.png` — the Fleet status view
- `copilot.png` — the AI Copilot view

Save the screenshots here with exactly those filenames so the README renders them.
