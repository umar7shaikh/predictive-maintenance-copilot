# ---- Web / API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- DB ----
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ---- Config / validation ----
pydantic==2.10.4
pydantic[email]==2.10.4
email-validator==2.2.0
pydantic-settings==2.7.0
python-dotenv==1.0.1

# ---- Auth ----
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# ---- Data / ML ----
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
scikit-learn==1.6.0
# Full mlflow for tracking + the local UI/server (mlflow server).
mlflow==2.19.0

# ---- LLM ----
groq==0.13.1

# ---- RAG (Phase 2) ----
# Embeddings via ChromaDB's built-in all-MiniLM-L6-v2 (ONNX Runtime) — no torch.
chromadb==0.5.23
pypdf==5.1.0

# ---- Async jobs + distributed ETL (Phase 4) ----
celery==5.4.0
redis==5.2.1
# PySpark ETL engine (ETL_ENGINE=spark); needs a JDK + winutils on Windows.
pyspark==3.5.3

# ---- Sample data / PDF generation ----
fpdf2==2.8.2

# ---- Tests ----
pytest==8.3.4
httpx==0.28.1
